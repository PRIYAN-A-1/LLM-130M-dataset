Tiny synthetic demonstration only: 50 optimizer steps on the included synthetic sample with configs/tiny.yaml. Not a 130M checkpoint and not a useful pretrained assistant. Try: python -m llm generate --checkpoint demo/latest.pt --prompt "Priyan learns" --max-new-tokens 20. Output may be nonsensical. Optimizer state is included for inspection, but prepare matching data and use original configuration to resume.
