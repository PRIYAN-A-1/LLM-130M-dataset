Use UTF-8 TXT (one document per nonempty line) or JSONL with a text string. No automatic corpus downloads. Preparation streams documents into uint32 token files. Identical document strings hash to the same split, but duplicates are NOT removed. Clean and deduplicate your corpus before preparation. Train the tokenizer on training-only documents for strict held-out evaluation. Avoid private data without consent and retain dataset license/source records. SFT JSONL requires instruction and output, with optional input. Parquet is not implemented; convert it to JSONL upstream. SFT currently loads usable examples into RAM.
